package server

// Moves the cargo in a warehouse so that all the empty bays are at the end of
// the list.
func (p *Player) CmdShuffle(planetName string) {
	p.Output("Not implemented. Check back in 2 weeks.\n")
}
